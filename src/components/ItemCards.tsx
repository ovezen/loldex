import React from "react";
import { Item } from "@/types/Item";

interface ItemCardProps {
  item: Item;
}

const ChampionCards = ({ item }: ItemCardProps) => {
  return (

<div>hello</div>
  );
};

export default ChampionCards;
